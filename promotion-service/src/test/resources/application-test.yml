# src/test/resources/application-test.yml
spring:
  application:
    name: promotion-service-test

  r2dbc:
    url: r2dbc:h2:mem:///testdb;DB_CLOSE_DELAY=-1
    username: sa
    password:

  sql:
    init:
      mode: always

  kafka:
    bootstrap-servers: localhost:9092

eureka:
  client:
    enabled: false  # Desabilita Eureka nos testes

logging:
  level:
    root: WARN
    com.ecommerce: DEBUG